import { Wrench, FileText, Droplet, Calendar, Star } from 'lucide-react';
import type { LucideIcon } from 'lucide-react';

export const iconMap: Record<string, LucideIcon> = {
  Wrench,
  FileText,
  Droplet,
  Calendar,
  Other: Star,
};

export default iconMap;
